<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />

    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />


    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@500&display=swap" rel="stylesheet">

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

    <link rel="stylesheet" href="styles/style.css" />
    <title>Custom To-Do List</title>
</head>
<body>

    <div class="mainContainer">

    <header>
        <div class="nav">
            <div class="navItems div-with-underline">
                <span class="material-symbols-outlined pinkText">
                    today
                </span>
                <a href="/">Today</a>
            </div>
    
            <div class="navItems div-with-underline" style="border-right: 2px solid #fa6985; padding-right: 20px;">
                <span class="material-symbols-outlined pinkText">
                    business_center
                </span>
                <a href="/Work">Work</a>
            </div>

            <!-- Custom lists -->
            <%if(customListsArray)
            {
                customListsArray.forEach((listName)=>
                {%>
                    <div class="navItems div-with-underline">
                        <form action="/deleteList" method="POST">
                            <label class="customList-label">
                                <input type="checkbox" class="myCheckbox custom-checkbox" name="listCheckbox" value="<%= listName %>" onchange="this.form.submit()"/>
                                <a class="strikeMe" href="/<%= listName %>"><%= listName.charAt(0).toUpperCase() + listName.slice(1)%></a>  
                            </label>
                        </form>
                    </div>
               <% })
            }%>

            <!-- Add new to-do list -->
            <div class="navItems">
                <form  action="/customList" method="POST">
                    <input class="newlist" type="text" name="customList" placeholder="Add new to-do list" required/>
                    <button class="round-button" type="submit">+</button>
                  </form>
            </div>

        </div>

    </header>
    
